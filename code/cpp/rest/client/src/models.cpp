// Copy from server models.cpp for consistency
// This would normally be a shared library in a real project

#include "../server/include/models.h"

// The implementation would be identical to the server's models.cpp
// For brevity, this demonstrates that models can be shared between client and server